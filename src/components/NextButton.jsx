import React from "react";
import { Button } from "react-bootstrap";

export default function NextButton() {
  return (
    <div className="mt-5">
      <Button variant="warning" className="next w-25">
        Next
      </Button>
    </div>
  );
}
